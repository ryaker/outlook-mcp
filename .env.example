# Microsoft Azure App Registration
MS_CLIENT_ID=
MS_CLIENT_SECRET=
MS_TENANT_ID=common

# MCP transport: "stdio" (local) or "http" (remote / Cowork)
MCP_TRANSPORT=stdio

# Only used when MCP_TRANSPORT=http
HTTP_PORT=3333
HTTP_HOST=0.0.0.0
